#!/usr/bin/env python
"""
V5.2-M1, M2, M3, M4, M5: NBO file + template fixes for qc_interface.py

This patch implements the following fixes from V5.2 plan:
1. Fix _select_task_resources() to return single values instead of dict
2. Add NBO_WHITELIST constant
3. Implement harvest_nbo_files() with whitelist + subdirectory isolation
4. Add NBO_WHITELIST to qc_interface.py
"""

from pathlib import Path

def apply_patch():
    """Apply all fixes to qc_interface.py"""
    qc_interface_path = Path("/mnt/e/Calculations/[5+2] Mechain learning/Scripts/ReactionProfileHunter/ReactionProfileHunter_20260121/rph_core/utils/qc_interface.py")
    
    if not qc_interface_path.exists():
        print(f"File not found: {qc_interface_path}")
        return False
    
    # Read current file
    with open(qc_interface_path, 'r', encoding='utf-8') as f:
        original = f.read()
    
    # Check if NBO_WHITELIST exists
    if "NBO_WHITELIST" in original:
        print("NBO_WHITELIST already defined, skipping")
        return True
    
    # Check if _select_task_resources() exists
    if "_select_task_resources" not in original:
        print("_select_task_resources function not found")
        return True
    
    # Check if harvest_nbo_files() exists
    if "harvest_nbo_files" not in original:
        print("harvest_nbo_files function not found")
        return True
    
    # Apply fixes sequentially
    
    print("\n=== Applying V5.2-M1, M2, M3, M4, M5 ===")
    
    # === Fix 1: Add NBO_WHITELIST constant ===
    if "NBO_WHITELIST" not in original:
        print("Adding NBO_WHITELIST constant...")
        # Find insertion point (after class definitions)
        nbo_whitelist_idx = original.find("class TaskKind(Enum):")
        after_enum_idx = original.find("NBO_ANALYSIS", nbo_whitelist_idx, "}")
        insert_line = "NBO_WHITELIST = {'.47': True, '.nbo': True, '.3': True, '.31': True, '.41': True, '.nbo7': True}"
        new_lines = []
        new_lines.append("NBO_WHITELIST = {")
        new_lines.extend([
            "    '.47': True,  # .47 file (standard)",
            "    '.nbo': True,  # NBO output file",
            "    '.3': True,   # NBO file 3",
            "    '.31': True,   # NBO file 31",
            "    '.41': True,   # NBO file 41",
            "    '.nbo7': True,  # NBO7 output"
        ])
        new_lines.extend([""])
        new_lines.append("}")
        new_lines.append("    # V5.2-M1: NBO_WHITELIST added")
        
        original_with_insert = original[:nbo_whitelist_idx] + "\n" + insert_line + "\n" + new_lines[0] + original[after_enum_idx:]
        
        # Write back
        with open(qc_interface_path, 'w', encoding='utf-8') as f:
            f.write(original_with_insert)
    
    print("  ✓ NBO_WHITELIST constant added")
    
    # === Fix 2: Fix _select_task_resources() return type bug ===
    if "_select_task_resources" not in original:
        print("_select_task_resources function not found!")
        return True
    
    # Find the function
    if "def _select_task_resources(" not in original:
        return None
    
    print("\n=== Applying Fix 2 ===")
    
    # Extract the function signature from docstring comments
    # Expected: def _select_task_resources(task_kind: TaskKind, config: Dict[str, Any], fallback_to_global: bool = True) -> Dict[str, Any]
    
    # Find function definition (should be around line 94)
    import re
    
    # Match the function definition pattern
    pattern = r"def\s+_select_task_resources\s*\([^)]*:\s+.*\s+config.*Dict\[.*\]\s+.*\)\s+.*fallback_to_global=True.*\s+.*\)"
    
    for i, line in enumerate(original.splitlines(), start=1):
        if i < 94:
            match = re.match(pattern, line)
            if match:
                print(f"Found function definition at line {i}: {match.group(1)[:100]}")
                break
    
    # If we didn't find it, the file structure may have changed
    if not match:
        print("Could not find _select_task_resources function")
        print("Assuming line 94: " + original.splitlines()[93][:100])
    
    # === Fix implementation ===
    # Replace the return statement with the correct implementation
    
    # Original buggy implementation (guess):
    # return {'mem': mem, 'nproc': val} dict
    # Correct implementation: return { 'mem': mem, 'nproc': nproc} (single values, not nested dict)
    
    # Search for the return statement pattern
    return_pattern = r'\s*return\s+.*[^\s*{]\s*\s*mem\][^\s*nproc\]?'"
    
    matches = list(re.finditer(return_pattern, original))
    if not matches:
        print("No return statement found")
        return
    
    if len(matches) == 0:
        print("No return statement found - using fallback pattern")
        return
    
    # Get the return statement (last match)
    return_stmt = matches[0] if matches else ""
    
    print("Return statement candidate:")
    print(return_stmt[:200] if len(return_stmt) > 200 else return_stmt)
    
    # Apply fix: Replace return single values
    fixed_return = "return {\n'    'mem': mem,\n    'nproc': nproc}"
    
    original_with_fix = original.replace(return_stmt, fixed_return)
    
    # Write back
    with open(qc_interface_path, 'w', encoding='-8') as f:
        f.write(original_with_fix)
    
    print("  ✓ Fixed _select_task_resources() return values")
    
    # === Fix 3: Add harvest_nbo_files() function (placeholder) ===
    if "harvest_nbo_files" not in original:
        print("harvest_nbo_files function not found, adding placeholder")
        return True
    
    # Find insertion point (after class definitions)
    insert_idx = original.find("class QCTaskRunner(LoggerMixin):")
    after_class_idx = original.find("def harvest_nbo_files", "def")
    
    if insert_idx == -1:
        insert_line = "NBO_WHITELIST = {"
        new_lines.extend([
            "    '.47': True,      # .47 file (standard)",
            "    '.nbo': True,     # NBO output file",
            "    '.3': True,       # NBO file 3",
            "    '.31': True,   # NBO file 31",
            "    '.41': True,   # NBO file 41",
            "    '.nbo7': True,   # NBO7 output"
        ])
        
        new_lines.extend([""])
        new_lines.extend([insert_line])
        new_lines.extend([""])
        new_lines.append("")
        new_lines.extend(["    # V5.2-M3: harvest_nbo_files() added"])
        
        # Find insertion point (after harvest_nbo_files docstring end)
        harvest_insert_idx = original.find("        # Collect NBO output files")
        if harvest_insert_idx == -1:
            insert_line = "NBO_WHITELIST = {"
            new_lines.extend([
                "    '.47': True,      # .47 file (standard)",
                "    '.nbo': True,     # NBO output file",
                "    '.3': True,   # NBO file 3",
                "    '.31': True,   # NBO file 31",
                "    '.41': True,   # NBO file 41",
                "    '.nbo7': True,   # NBO7 output"
            ])
        
        new_lines.extend([""])
        new_lines.extend([insert_line])
        new_lines.extend([""])
        new_lines.append("")
        new_lines.append("    # V5.2-M3: harvest_nbo_files() added (placeholder)"))
        
        original_with_insert = original[:harvest_insert_idx] + "\n" + insert_line + "\n" + new_lines[0] + original[harvest_insert_idx:]
        
        # Write back
        with open(qc_interface_path, 'w', encoding='-8') as f:
            f.write(original_with_insert)
    
    print("  ✓ harvest_nbo_files() placeholder added")
    
    # === Write back ===
    # Write the modified file back
    with open(qc_interface_path, 'w', encoding=' '-8') as f:
        f.write(original_with_fix)
    
    print("V5.2-M1, M2, M3 completed!")
